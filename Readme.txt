- run TimeTracker.java class.
- for Task1 every time you type a name to add a new name delete the previous
name. Also, if you want and det another start time and end time just click on the
start and work button. To clear the list just open and close the UI(is adding multiple names).
- for Task2 When entering a new project name, due date and due time just delete the
previous text(if adding multiple projects). When
- for task3 just enter a time in minutes and when the time count down from the time then display "Times up!". Then if you
want to enter another time delete the previous text.
- All the UIs does not handle resizing.